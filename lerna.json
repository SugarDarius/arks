{
    "version": "1.0.0",
    "npmClient": "npm",
    "packages": [
        "packages/*"
    ],
    "command": {
        "version": {
            "allowBranch": "master"
        },
        "publish": {
            "ignoreChanges": [ "*.md" ],
            "message": "build(@arks): publish release %s",
            "registry": "https://registry.npmjs.org/"
        }
    }
}