This the list of all options availables in the **arks.json** file.

```json
{
    "appName": "string",
    "noMetrics": boolean,
    "noLiveness": boolean,
    "metricsEndpoint": "string",
    "livenessEndpoint": "string",
    "noHelmet": boolean,
    "noCors": boolean,
    "noLimit": boolean,
    "noBodyParser": boolean,
    "noCookieParser": boolean,
    "noCompression": boolean,
    "noLogger": boolean,
}
```